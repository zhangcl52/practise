package com.hunter.study.algorithm;import org.junit.Test;import java.util.Arrays;/** * @Author 水木 * @Date 2021/2/19  12:49 下午 */public class SortUtil {	/**	 * 产生最大Size为[1，maxSize]的数组，数组中的值(-maxValue,maxValue]	 *	 * @param maxSize	 * @param maxValue	 * @return	 */	public static int[] generateRandomArray(int maxSize, int maxValue) {		//Math.random() 取值范围 [0，1）		//Math.random() + 1) * maxSize  取值范围 [0，maxSize]		int[] arr = new int[(int) (maxSize * Math.random()) + 1];		for (int i = 0; i < arr.length; i++) {			arr[i] = (int) ((maxValue + 1) * Math.random()) - (int) (maxValue * Math.random());		}		return arr;	}	/**	 * 产生固定size大小的数组，数组中的值[0,maxValue]	 *	 * @param size	 * @param maxValue	 * @return	 */	public static int[] generateFixSizeRandomArray(int size, int maxValue) {		return generateFixSizeRandomArray(size, 0, maxValue);	}	/**	 * 产生固定size大小的数组，数组中的值[minValue,maxValue]	 *	 * @param size	 * @param maxValue	 * @return	 */	public static int[] generateFixSizeRandomArray(int size, int minValue, int maxValue) {		//Math.random() 取值范围 [0，1）		int[] arr = new int[size];		for (int i = 0; i < arr.length; i++) {			arr[i] = (int) ((maxValue - minValue + 1) * Math.random()) + minValue;		}		return arr;	}	/**	 * arr数组中 index1的值和index2的值进行交换	 *	 * @param arr	 * @param index1	 * @param index2	 * @return	 */	public static int[] swapInArray(int[] arr, int index1, int index2) {		if (arr == null) {			return null;		}		if (index1 > arr.length - 1 || index2 > arr.length - 1) {			return null;		}		int temp = arr[index1];		arr[index1] = arr[index2];		arr[index2] = temp;		return arr;	}	@Test	public void generateFixSizeRandomArrayTest() {		for (int i = 0; i < 10; i++) {			System.out.println(Arrays.toString(SortUtil.generateFixSizeRandomArray(3, 100)));		}	}	@Test	public void generateRandomArrayTest() {		for (int i = 0; i < 10; i++) {			System.out.println(Arrays.toString(SortUtil.generateRandomArray(5, 4)));		}	}	@Test	public void swapInArrayTest() {		for (int i = 0; i < 10; i++) {			int[] before = SortUtil.generateFixSizeRandomArray(5, 100);			System.out.println("==========第" + i + "组=========");			System.out.println("before=" + Arrays.toString(before));			SortUtil.swapInArray(before, 0, 1);			System.out.println("after=" + Arrays.toString(before));		}	}}