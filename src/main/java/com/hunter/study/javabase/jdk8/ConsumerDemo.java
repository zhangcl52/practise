package com.hunter.study.javabase.jdk8;import java.util.function.Consumer;/** * @Author 水木 * @Date 2019-07-09  10:13 * <p> * 输入一个，不进行输出 */public class ConsumerDemo {    public static void main(String[] args) {        //先定义一个consumer的实现类，实现了accept方法        Consumer consumer = n -> System.out.println(n + " 你好");        consumer.accept("水木");        //直接调用方法,第一个参数是需要处理的字符串，第二个是处理第一个字符串的方法（可以理解为一个实现了Consumer接口的对象）        soutName1("水木", System.out::println);        soutName2("水木", n -> System.out.println(n + " 你很好"), n -> System.out.println(n + " 你很累"));    }    /**     * @param name 为需要处理的参数     * @param con  是处理参数的方法     */    private static void soutName1(String name, Consumer<String> con) {        con.accept(name);    }    /**     * 按顺序从左向右执行     *     * @param name     * @param con1     * @param con2     */    private static void soutName2(String name, Consumer<String> con1, Consumer<String> con2) {        con1.andThen(con2).accept(name);    }}